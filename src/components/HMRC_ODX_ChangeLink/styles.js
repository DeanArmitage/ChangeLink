import styled, { css } from 'styled-components';

export default styled.div(() => {
  return css`
    margin: 0;
    /* align-items: flex-start;
    justify-content: end;
    display: flex;
    flex: 30%; */
  `;
});
